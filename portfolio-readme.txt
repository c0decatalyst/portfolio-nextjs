If having ip lookup issues/DNS, cehck out /etc/gai.conf for the precedence 96
Install dependencies
    npm install create-next-app@latest --ts
    npm install -D tailwindcss postcss autoprefixer
    npx tailwindcss init -p
    